apiVersion: eksctl.io/v1alpha5
kind: ClusterConfig

metadata:
  name: new-cluster
  region: us-east-2

nodeGroups:
  - name: ng-1
    instanceType: t2.micro
    desiredCapacity: 2
    volumeSize: 20

  - name: ng-2
    instanceType: t2.micro
    desiredCapacity: 2
    volumeSize: 20